# P0 验收矩阵

以下全部是 P0。文档状态：`ACTIVE VERIFICATION — 不因后端局部通过而声称整体 MVP 完成`。

此前的验收表在产品文档冻结前就把全部条目标记为 `PASS`，这些标签现已撤回，不能继续作为完成声明。当前状态 `待重新验收` 表示：该验收信号是强制的；历史实现可能存在相关证据，但必须基于冻结后的文档，从干净压缩包和用户实际入口重新执行。完成重新验收前，任何单项或整个 MVP 都不能声称完成。

| 编号 | 状态 | 必须取得的验收信号及需要重查的历史证据 |
|---|---|---|
| P0-01 干净环境可运行 | 历史后端包通过；当前最终包待复跑 | 收口前源码包曾在无 Git/依赖/构建/运行状态的目录完成 `npm ci`、verify、E2E、三连跑、smoke、audit；当前工作树已继续变更，最终冻结后必须对新包重跑，不能沿用旧哈希。`.env.example` 和一键离线案例具备。 |
| P0-02 五个真实状态 | 后端与本地 E2E 通过 | 五状态、输出消费、失败传播、持久化、问题失配均在 163/163 Node 测试与 1/1 Playwright E2E 中通过；光伏问题只返回对应 EvidenceGap，不泄漏新能源车结论。 |
| P0-03 四类真实工具 | 通过 | 同一黄金任务记录快照搜索、PDF、CSV 计算、PPTX，另记录模型 PLAN/SYNTHESIZE；上传经 `local-file-reader` 进入 COLLECT。三种模式字段进入运行对象和 JSON。 |
| P0-04 结论追溯到底稿 | 通过 | 4/4 结论具有证据路径或 EvidenceGap；Zod 交叉引用校验会拒绝形状正确但引用伪造的运行对象。 |
| P0-05 机器可读类型 | 后端通过，前端待展示 | 五个正交轴已存储并验证；编辑保持待复核。当前前端尚未完整展示 `originType`、规范化审阅状态与 freshness。 |
| P0-06 冲突与证据不足 | 通过 | 双值、定位、候选解释、缺口、确认阻断和 PPTX/JSON 保留均有测试。 |
| P0-07 Audit→Repair | 通过 | 六类规则均留下执行结果；补证据后审计结果改变，未知引用会被移除；语义降级会同步创建/连接 EvidenceGap，并保留已解决缺口历史；只链接既有 Assumption；最多修复一轮。 |
| P0-08 人负责最终判断 | 后端通过，前端阻塞 | 后端已把 EDIT 与 CONFIRM 分离，保留 CandidateRevision，并要求冲突/估算/预测填写理由和范围；当前前端仍显示“保存并确认”且没有理由/范围输入。 |
| P0-09 来源变化影响链 | 通过 | SourceVersion v1/v2、选择性 STALE、决定失效、重算、不可变成果快照和按版本历史下载均有集成测试；同一后端合同覆盖缓存模型与在线单端点模型的动态 Claim/Conclusion ID，重启恢复测试证明前端无需保存影子状态。多结论对抗测试进一步证明：更新保留每条结论原有的规范化证据状态，不向只引用单一 CSV 的路径注入未引用的冲突数据。 |
| P0-10 三个真实成果 | 本地通过，Windows 待验 | 交互报告及同一证据快照的 MD/PDF/五页 OOXML PPTX/证据 JSON 已生成并由真实解析器检查；PPTX 已渲染并在 macOS PowerPoint 编辑，Windows Microsoft PowerPoint 仍需目标环境。 |
| P0-11 现场稳定运行 | 本地通过；最终 ZIP 待复跑 | 本轮三连跑 3/3（88/20/16 ms）、smoke 与 Playwright E2E 1/1（5.9 s）通过；最终源码 ZIP 仍须在干净解压目录复跑。 |
| P0-12 最低安全边界 | 后端通过，前端接线待完成 | 上传字节/路径/SHA/篡改、loopback、CSP、工具白名单、注入、密钥扫描已测；后端 `uploadIds`→COLLECT 通过。统一 `MAX_SOURCES=10` 覆盖三搜索引擎、单提供方、快照与运行对象，第 11 条候选被截断，搜索响应、COLLECT 摘要和证据包留存机器记录。搜索仅 HTTP/HTTPS，fetch 前校验精确 host 与全部 DNS 地址并拒绝环回/私有/保留地址；默认 fetch 再解析的 DNS TOCTOU 风险仍开放。当前前端未把已上传 ID 带入运行请求。 |

## 文档冻结后必须重新执行的量化检查

- 候选结论：4 条。
- 结论责任覆盖：4/4 具有支持路径或 `EvidenceGap`（100%）；所有有支撑的引用均可定位。
- 引用定位：黄金案例全部证据路径都能到达原始位置。
- 确定性计算：保留公式、输入、单位、期间和 CSV 行号。
- 无支撑但已确认的结论：0 条。
- 自动修复次数：1 次。
- 黄金 E2E 外部请求：0 次。
- PPTX：5 页；包含可编辑文本元素；溢出问题为 0。
- 后端覆盖率：c8 对 `src/**` 语句、分支、函数与行均为 100%；当前全量 Node 测试 163/163。
- 随机用例/生产源码：694,100/6,931 = 100.14:1；物理 Node/fuzz 测试/生产 TypeScript 行为 6,926/6,931 = 0.999:1，计入 E2E 后为 7,023/6,931 = 1.013:1；这些口径不得混写。
- 实时信源发现：2026-08-28 已对 Bing、Google、百度各执行一次真实只读查询；三者因本机 DNS 分别解析到 `198.18.1.116/.68/.123` 而在 fetch 前 fail-closed。失败原文与 DNS 观测已保存；当前环境没有新的成功结果，不能用模拟或历史成功替代。进入 COLLECT 解析和建模前，结果只能是候选来源。
- 固定白名单核验：必须核验当前白名单响应并保存哈希；任何局部失败持续保留为失败，并与信源发现严格区分。
- 上传覆盖：接受 PDF/CSV/TXT/XLSX；拒绝伪装 PDF、异常 XLSX、路径穿越文件名、NUL/非法 UTF-8 文本和超过 5 MiB 的请求体；持久化文件被篡改时返回 409。
- 问题失配覆盖：输入无关的光伏问题时，3/3 返回 `INSUFFICIENT_EVIDENCE`，不泄漏新能源车结论。
- 在线 LLM：2026-08-28 环境检查确认 API Key、Base URL、Model 三项均不存在，因此没有发起在线请求，也没有用缓存/模拟替代。单端点 PLAN/SYNTHESIZE 的真实外网成功仍未验证；含未知 evidence ID 的单条候选会整体丢弃（不删除坏 ID 后部分放行），其他独立候选继续校验，过滤后少于 3 条使整个 SYNTHESIZE 失败；不得自动切换/降级模型已有本地测试。
- 历史在线 LLM 证据不能证明默认黄金路径消费了真实模型缓存。最终验收必须在 UI 和导出的运行对象中证明候选来源。
- 所有者要求的环境证据：浏览器上传闭环、单一提供方实时搜索、固定白名单实时核验、单端点在线 LLM、Windows Microsoft PowerPoint 打开/编辑/保存/重开，必须分别报告；离线或本地替代不能关闭这些风险。公开部署风险也必须最终核验，但在获得针对具体平台的单独授权前不得执行。

## 必须执行的反证测试

- 把黄金问题改成无关的光伏出口问题，逐项检查计划、每条候选文本和每条证据路径。
- 为原本证据不足的 Claim 补入有效支撑；Audit 不得继续输出相同的“无支撑”发现。
- 删除引用，并把一个 evidence ID 替换成未知 ID；SYNTHESIZE/Audit 必须 fail-closed。
- 上传一个合法文件，读取持久化字节，并比较浏览器、POST、GET 的 SHA-256；如果 UI 写“加入研究”，必须证明文件进入 COLLECT，否则拒绝该文案。
- 在来源中同时放入中文和英文提示词注入文本；计划、工具清单、环境访问和最终状态不得改变。
- 从最终 ZIP 的干净解压目录连续运行三次，不能使用工作树。
- 确认或编辑一条候选，再应用内置 v2；验证每个动作都会创建新的 `ArtifactVersion`，且旧决定和旧成果仍可检查。
- 对 Path 1–6 分别执行边界 preset、节点失败、非法确认、未完成边界请求、旧版本改写和混合公网/私网 DNS 响应；任一路径不得把失败伪装为成功。

## 最终包必须执行的命令

```bash
npm run verify
npm run contract:check # 可单独重跑；verify 已强制包含此项
npm run fuzz # 可单独重跑；verify 已强制包含此项
npm run test:e2e
npm run demo:triple
npm run smoke
npm audit --audit-level=high
```
