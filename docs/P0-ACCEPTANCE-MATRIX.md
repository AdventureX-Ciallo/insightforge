# P0 验收矩阵

以下全部是 P0。文档状态：`DRAFT — 完成结论已重开`。

此前的验收表在产品文档冻结前就把全部条目标记为 `PASS`，这些标签现已撤回，不能继续作为完成声明。当前状态 `待重新验收` 表示：该验收信号是强制的；历史实现可能存在相关证据，但必须基于冻结后的文档，从干净压缩包和用户实际入口重新执行。完成重新验收前，任何单项或整个 MVP 都不能声称完成。

| 编号 | 状态 | 必须取得的验收信号及需要重查的历史证据 |
|---|---|---|
| P0-01 干净环境可运行 | 待重新验收 | 从最终压缩包执行：`npm ci`、全部测试、生产构建和启动；无需真实密钥；提供 `.env.example`；提供一键离线案例；不存在凭据或真实用户数据。 |
| P0-02 五个真实状态 | 待重新验收 | 页面展示状态轨道并持久化 `RunStep[]`；前一步输出哈希被下一步消费；失败正确传播；修改问题后，计划与候选结论发生实质变化。输入无关问题时只能返回与该问题相关的证据缺口，不能泄漏黄金案例结论。 |
| P0-03 四类真实工具 | 待重新验收 | 一条自然任务中真实调用 `snapshot-search`、`pdf-reader`、`csv/xlsx-calculator`、`pptx-generator`；事件包含完整七字段；每个工具输出进入任务或证据链。`sourceDiscoveryMode`、`authorityVerificationMode`、`synthesisMode` 分别持久化到 UI、运行对象和导出成果。 |
| P0-04 结论追溯到底稿 | 待重新验收 | 产生 3–5 条结论；100% 具有支持路径（`Conclusion→Claim→Evidence/Datum→Source→locator`）或明确的 `EvidenceGap`；有支撑的引用最多两次操作可定位；计算可精确复算。 |
| P0-05 机器可读类型 | 待重新验收 | 使用相互正交的 Zod/JSON 状态轴保存事实、来源观点、计算、估算、预测，责任来源（如 `AI_JUDGMENT`）、证据状态、审阅状态（如 `HUMAN_CONFIRMED`）和新鲜度；估算/预测带假设或区间；编辑不会自动确认。 |
| P0-06 冲突与证据不足 | 待重新验收 | 同时保留冲突值和各自定位；解释保持候选状态；证据不足判断列出具体缺口、禁止确认，并持续出现在报告、PPTX 和 JSON 中。 |
| P0-07 Audit→Repair | 待重新验收 | 覆盖六类审查；Audit 读取当前结构化输入；展示修正前后差异；自动修复最多一次。补入证据、删除引用或修改数值后，审查结果必须相应变化；不得依赖固定 ID 播放剧本。 |
| P0-08 人负责最终判断 | 待重新验收 | 支持确认、编辑、驳回、查看依据；保留原始 AI 版本和决定时间；编辑与确认是两个动作；确认冲突、估算或预测时记录理由和范围；证据不足或过期结论禁止确认；人工动作后刷新成果版本。 |
| P0-09 来源变化影响链 | 待重新验收 | 明确标记内置 v1→v2 演示更新；准确列出受影响对象；相关人工决定保留但失效；重新计算并生成新成果版本；不相关结论保持不变。 |
| P0-10 三个真实成果 | 待重新验收 | 交互报告、五页可编辑 OOXML PPTX、包含必需字段和三种独立模式字段的证据 JSON。PPTX 必须在声明的目标 Microsoft PowerPoint 环境中完成打开、编辑、保存和重开。 |
| P0-11 现场稳定运行 | 待重新验收 | 离线模式如实标记；展示真实进度；连续三次运行成功；单次不超过三分钟、目标 90 秒；五分钟内讲完；灾备 PPTX 不代替正常执行。 |
| P0-12 最低安全边界 | 待重新验收 | 存在真实用户上传入口；完成“浏览器→POST→持久化 GET”的 SHA-256 闭环；校验类型、大小、字节和目录边界；安全 DOM；仅监听 loopback；工具白名单；中英文注入材料保持惰性；日志和 ZIP 通过密钥扫描。只有独立 validator、不连接真实入口不能通过。 |

## 文档冻结后必须重新执行的量化检查

- 候选结论：4 条。
- 结论责任覆盖：4/4 具有支持路径或 `EvidenceGap`（100%）；所有有支撑的引用均可定位。
- 引用定位：黄金案例全部证据路径都能到达原始位置。
- 确定性计算：保留公式、输入、单位、期间和 CSV 行号。
- 无支撑但已确认的结论：0 条。
- 自动修复次数：1 次。
- 黄金 E2E 外部请求：0 次。
- PPTX：5 页；包含可编辑文本元素；溢出问题为 0。
- 实时信源发现：必须保存一次单一提供方真实成功和一次配置/网络失败；进入 COLLECT 解析和建模前，结果只能是候选来源。
- 固定白名单核验：必须核验当前白名单响应并保存哈希；任何局部失败持续保留为失败，并与信源发现严格区分。
- 上传覆盖：接受 PDF/CSV/TXT/XLSX；拒绝伪装 PDF、异常 XLSX、路径穿越文件名、NUL/非法 UTF-8 文本和超过 5 MiB 的请求体；持久化文件被篡改时返回 409。
- 问题失配覆盖：输入无关的光伏问题时，3/3 返回 `INSUFFICIENT_EVIDENCE`，不泄漏新能源车结论。
- 在线 LLM：必须保存单端点 PLAN/SYNTHESIZE 的一次成功，以及配置、网络或 Schema 失败；未知 evidence ID 使整份草稿失败；有效草稿少于 3 条时 SYNTHESIZE 失败；不得自动切换或降级模型。
- 历史在线 LLM 证据不能证明默认黄金路径消费了真实模型缓存。最终验收必须在 UI 和导出的运行对象中证明候选来源。
- 所有者要求的环境证据：浏览器上传闭环、单一提供方实时搜索、固定白名单实时核验、单端点在线 LLM、Windows Microsoft PowerPoint 打开/编辑/保存/重开，必须分别报告；离线或本地替代不能关闭这些风险。公开部署风险也必须最终核验，但在获得针对具体平台的单独授权前不得执行。

## 必须执行的反证测试

- 把黄金问题改成无关的光伏出口问题，逐项检查计划、每条候选文本和每条证据路径。
- 为原本证据不足的 Claim 补入有效支撑；Audit 不得继续输出相同的“无支撑”发现。
- 删除引用，并把一个 evidence ID 替换成未知 ID；SYNTHESIZE/Audit 必须 fail-closed。
- 上传一个合法文件，读取持久化字节，并比较浏览器、POST、GET 的 SHA-256；如果 UI 写“加入研究”，必须证明文件进入 COLLECT，否则拒绝该文案。
- 在来源中同时放入中文和英文提示词注入文本；计划、工具清单、环境访问和最终状态不得改变。
- 从最终 ZIP 的干净解压目录连续运行三次，不能使用工作树。
- 确认或编辑一条候选，再应用内置 v2；验证每个动作都会创建新的 `ArtifactVersion`，且旧决定和旧成果仍可检查。

## 冻结后计划执行的命令

```bash
npm run verify
npm run test:e2e
npm run demo:triple
npm run smoke
npm audit --audit-level=high
```
