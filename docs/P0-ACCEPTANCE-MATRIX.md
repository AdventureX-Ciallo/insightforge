# P0 验收矩阵

以下全部是 P0。文档状态：`ACTIVE VERIFICATION — 不因后端局部通过而声称整体 MVP 完成`。

此前的验收表在产品文档冻结前就把全部条目标记为 `PASS`，这些标签现已撤回，不能继续作为完成声明。当前状态 `待重新验收` 表示：该验收信号是强制的；历史实现可能存在相关证据，但必须基于冻结后的文档，从干净压缩包和用户实际入口重新执行。完成重新验收前，任何单项或整个 MVP 都不能声称完成。

| 编号 | 状态 | 必须取得的验收信号及需要重查的历史证据 |
|---|---|---|
| P0-01 干净环境可运行 | 后端包通过 | 无 Git/依赖/构建/运行状态的源码包预检目录完成 `npm ci`、verify、E2E、三连跑、smoke、audit；`.env.example` 和一键离线案例具备。 |
| P0-02 五个真实状态 | 后端通过；浏览器复跑环境阻断 | 五状态、输出消费、失败传播、持久化、问题失配均有 91/91 Node 测试；光伏问题只返回对应 EvidenceGap，不泄漏新能源车结论。此前 E2E 通过，本轮沙箱在断言前阻断 Chromium 启动。 |
| P0-03 四类真实工具 | 通过 | 同一黄金任务记录快照搜索、PDF、CSV 计算、PPTX，另记录模型 PLAN/SYNTHESIZE；上传经 `local-file-reader` 进入 COLLECT。三种模式字段进入运行对象和 JSON。 |
| P0-04 结论追溯到底稿 | 通过 | 4/4 结论具有证据路径或 EvidenceGap；Zod 交叉引用校验会拒绝形状正确但引用伪造的运行对象。 |
| P0-05 机器可读类型 | 后端通过，前端待展示 | 五个正交轴已存储并验证；编辑保持待复核。当前前端尚未完整展示 `originType`、规范化审阅状态与 freshness。 |
| P0-06 冲突与证据不足 | 通过 | 双值、定位、候选解释、缺口、确认阻断和 PPTX/JSON 保留均有测试。 |
| P0-07 Audit→Repair | 通过 | 六类规则均留下执行结果；补证据后审计结果改变，未知引用会被移除；只链接既有 Assumption；最多修复一轮。 |
| P0-08 人负责最终判断 | 后端通过，前端阻塞 | 后端已把 EDIT 与 CONFIRM 分离，保留 CandidateRevision，并要求冲突/估算/预测填写理由和范围；当前前端仍显示“保存并确认”且没有理由/范围输入。 |
| P0-09 来源变化影响链 | 通过 | SourceVersion v1/v2、选择性 STALE、决定失效、重算、不可变成果快照和按版本历史下载均有集成测试。 |
| P0-10 三个真实成果 | 本地通过，Windows 待验 | 交互报告及同一证据快照的 MD/PDF/五页 OOXML PPTX/证据 JSON 已生成并由真实解析器检查；PPTX 已渲染并在 macOS PowerPoint 编辑，Windows Microsoft PowerPoint 仍需目标环境。 |
| P0-11 现场稳定运行 | 后端通过；浏览器复跑环境阻断 | 本轮三连跑 3/3（400/113/106 ms）与 smoke 通过；此前 E2E 3.4 秒。本轮 Chromium 因受管沙箱 Mach 权限在断言前退出，需在可启动浏览器的环境复跑。 |
| P0-12 最低安全边界 | 后端通过，前端接线待完成 | 上传字节/路径/SHA/篡改、loopback、CSP、工具白名单、注入、密钥扫描已测；后端 `uploadIds`→COLLECT 通过。搜索仅 HTTP/HTTPS，fetch 前校验精确 host 与全部 DNS 地址并拒绝环回/私有/保留地址。当前前端未把已上传 ID 带入运行请求。 |

## 文档冻结后必须重新执行的量化检查

- 候选结论：4 条。
- 结论责任覆盖：4/4 具有支持路径或 `EvidenceGap`（100%）；所有有支撑的引用均可定位。
- 引用定位：黄金案例全部证据路径都能到达原始位置。
- 确定性计算：保留公式、输入、单位、期间和 CSV 行号。
- 无支撑但已确认的结论：0 条。
- 自动修复次数：1 次。
- 黄金 E2E 外部请求：0 次。
- PPTX：5 页；包含可编辑文本元素；溢出问题为 0。
- 后端覆盖率：c8 对 `src/**` 行与分支均为 100%；当前全量 Node 测试 91/91。
- 测试/生产代码比例：3,095/5,019 = 0.617:1（物理 TypeScript 行）。
- 实时信源发现：必须保存一次单一提供方真实成功和一次配置/网络失败；进入 COLLECT 解析和建模前，结果只能是候选来源。
- 固定白名单核验：必须核验当前白名单响应并保存哈希；任何局部失败持续保留为失败，并与信源发现严格区分。
- 上传覆盖：接受 PDF/CSV/TXT/XLSX；拒绝伪装 PDF、异常 XLSX、路径穿越文件名、NUL/非法 UTF-8 文本和超过 5 MiB 的请求体；持久化文件被篡改时返回 409。
- 问题失配覆盖：输入无关的光伏问题时，3/3 返回 `INSUFFICIENT_EVIDENCE`，不泄漏新能源车结论。
- 在线 LLM：必须保存单端点 PLAN/SYNTHESIZE 的一次成功，以及配置、网络或 Schema 失败；未知 evidence ID 使整份草稿失败；有效草稿少于 3 条时 SYNTHESIZE 失败；不得自动切换或降级模型。
- 历史在线 LLM 证据不能证明默认黄金路径消费了真实模型缓存。最终验收必须在 UI 和导出的运行对象中证明候选来源。
- 所有者要求的环境证据：浏览器上传闭环、单一提供方实时搜索、固定白名单实时核验、单端点在线 LLM、Windows Microsoft PowerPoint 打开/编辑/保存/重开，必须分别报告；离线或本地替代不能关闭这些风险。公开部署风险也必须最终核验，但在获得针对具体平台的单独授权前不得执行。

## 必须执行的反证测试

- 把黄金问题改成无关的光伏出口问题，逐项检查计划、每条候选文本和每条证据路径。
- 为原本证据不足的 Claim 补入有效支撑；Audit 不得继续输出相同的“无支撑”发现。
- 删除引用，并把一个 evidence ID 替换成未知 ID；SYNTHESIZE/Audit 必须 fail-closed。
- 上传一个合法文件，读取持久化字节，并比较浏览器、POST、GET 的 SHA-256；如果 UI 写“加入研究”，必须证明文件进入 COLLECT，否则拒绝该文案。
- 在来源中同时放入中文和英文提示词注入文本；计划、工具清单、环境访问和最终状态不得改变。
- 从最终 ZIP 的干净解压目录连续运行三次，不能使用工作树。
- 确认或编辑一条候选，再应用内置 v2；验证每个动作都会创建新的 `ArtifactVersion`，且旧决定和旧成果仍可检查。
- 对 Path 1–6 分别执行边界 preset、节点失败、非法确认、未完成边界请求、旧版本改写和 DNS rebinding 攻击；任一路径不得把失败伪装为成功。

## 最终包必须执行的命令

```bash
npm run verify
npm run test:e2e
npm run demo:triple
npm run smoke
npm audit --audit-level=high
```
